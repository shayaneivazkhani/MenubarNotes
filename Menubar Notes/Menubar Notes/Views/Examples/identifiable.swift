//
//  identifiable.swift
//  Menubar Notes
//
//  Created by Shayan Eivaz Khani on 2025-07-15.


// WTF är en identifiable

/*
@State private var searchText: String = ""
@State private var prompt: String = "Prompt"

List {
    ForEach(Fruit.allCases.filter {
        searchText.isEmpty || $0.rawValue.contains(searchText)
    }) { fruit in
        Text(fruit.description)
    }
}.searchable(text: $searchText, prompt: prompt)

enum Fruit: String, Identifiable, CaseIterable, CustomStringConvertible {
    case apple
    case pear
    case banana
    case orange
    case blueberry
    case mango
    case pineapple
    case watermelon
    case grape
    case cherry

    var id: UUID { UUID() }
    var description: String { self.rawValue.localizedCapitalized }
}
*/
